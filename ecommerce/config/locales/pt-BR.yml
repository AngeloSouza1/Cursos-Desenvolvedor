pt-BR:
  hello: "Olá"
  activerecord:
    errors:
      messages:
        record_invalid: "A validação falhou: %{errors}"
        restrict_dependent_destroy:
          has_one: "Não pode excluir o registro porque um %{record} depende dele"
          has_many: "Não pode excluir o registro porque existem %{record} que dependem dele"
      models:
        # Aqui você pode adicionar mensagens específicas para seus modelos
        # Exemplo:
        # user:
        #   attributes:
        #     name:
        #       blank: "não pode ficar em branco"
      attributes:
        # Aqui você pode adicionar traduções específicas para os atributos dos seus modelos
        # Exemplo:
        # user:
        #   name: "Nome"
      format: "%{attribute} %{message}"
      messages:
        accepted: "deve ser aceito"
        blank: "não pode ficar em branco"
        confirmation: "não coincide com a confirmação"
        empty: "não pode ficar vazio"
        equal_to: "deve ser igual a %{count}"
        even: "deve ser par"
        exclusion: "não está incluído na lista"
        greater_than: "deve ser maior que %{count}"
        greater_than_or_equal_to: "deve ser maior ou igual a %{count}"
        inclusion: "não está incluído na lista"
        invalid: "não é válido"
        less_than: "deve ser menor que %{count}"
        less_than_or_equal_to: "deve ser menor ou igual a %{count}"
        model_invalid: "A validação falhou: %{errors}"
        not_a_number: "não é um número"
        not_an_integer: "deve ser um número inteiro"
        odd: "deve ser ímpar"
        other_than: "deve ser diferente de %{count}"
        present: "deve estar em branco"
        required: "é obrigatório"
        taken: "já está em uso"
        too_long:
          one: "é muito longo (máximo: 1 caractere)"
          other: "é muito longo (máximo: %{count} caracteres)"
        too_short:
          one: "é muito curto (mínimo: 1 caractere)"
          other: "é muito curto (mínimo: %{count} caracteres)"
        wrong_length:
          one: "não possui o tamanho correto (deve ter 1 caractere)"
          other: "não possui o tamanho correto (deve ter %{count} caracteres)"
        # Aqui você pode adicionar mais mensagens de erro personalizadas, se necessário
